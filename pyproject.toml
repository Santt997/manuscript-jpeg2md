[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "manuscript-jpeg2md"
version = "0.1.3"
description = "cast from manuscript-jpeg to md"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    
]

[tool.setuptools]
packages = []